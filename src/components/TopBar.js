import React from 'react'
import { NavItem, Toolbar } from 'rebass'

const TopBar = () =>
    <Toolbar>
        <NavItem>
            Contact
        </NavItem>
        <NavItem>
            About
        </NavItem>
    </Toolbar>

export default TopBar

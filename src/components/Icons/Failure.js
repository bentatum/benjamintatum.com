
import { Reflex } from 'reflexbox'
import { default as React } from 'react'
import { default as Icon } from 'react-icons/lib/md/close'

const Failure = (props) =>
  <Icon {...props} />

export default Reflex(Failure)
